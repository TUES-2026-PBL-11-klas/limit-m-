import React, { useState, useEffect, useRef } from 'react';
import {
  View,
  Text,
  TouchableOpacity,
  StyleSheet,
  SafeAreaView,
  KeyboardAvoidingView,
  Platform,
  NativeModules,
} from 'react-native';

const HistoryScreen = ({ navigation }) => {

    return ();
};
